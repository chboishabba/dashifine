# Plan 03-01: Archive Context Sync

## Goal

Resolve the user-supplied ChatGPT thread links into canonical archive records
and align repo context, TODOs, planning notes, and changelog entries with the
resulting thread-backed decisions.

## Constraints

- Use the canonical archive at `~/chat_archive.sqlite`; do not create a parallel archive.
- Update docs first; do not change repo code unless the docs update proves a helper is required.
- Keep public-facing README changes high level; no raw UUID dump there.

## Tasks

- [x] Deduplicate the supplied links to a 37-UUID canonical input set.
- [x] Resolve archive hits locally and live-pull the remaining UUIDs into the canonical archive.
- [x] Record `online UUID`, `canonical_thread_id`, `title`, `source`, and short topic notes in `COMPACTIFIED_CONTEXT.md`.
- [x] Update `TODO.md` with the actionable repo-facing follow-ups recovered from the synced threads.
- [x] Update `.planning/ROADMAP.md` and `.planning/STATE.md` so the planning docs reflect the archive/context-sync milestone.
- [x] Update `CHANGELOG.md` and add a small README note that reflects the same priorities at a public-facing level.

## Definition of Done

- All 37 unique UUIDs resolve in `~/chat_archive.sqlite`.
- Context, TODO, planning docs, README, and changelog all describe the same
  archive-backed project priorities.
- No repo code changes are required for this sync task.
