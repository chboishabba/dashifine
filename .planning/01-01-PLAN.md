# Plan 01-01: Scale Robustness Reporting

## Goal

Add a first-class scale-robustness report that measures the pos_scale interval length where cone_frac_min >= threshold, per label and overall. Enforce nontriviality (indefinite masks only) and support perturbation/arrow variants.

## Constraints

- Do not change existing embedding definitions.
- Do not break existing CLI defaults in 31/32 scripts.
- Do not install dependencies.

## Tasks

- [x] Implement a new robustness sweep script with:
  - [x] Required mask input and indefinite mask validation.
  - [x] Sweep over pos_scale range and compute per-label + overall interval lengths.
  - [x] Threshold control (default 1.0, optional 0.99).
  - [x] Output CSVs for summary + per-label intervals.
  - [x] Optional perturbation variants (noise, resample, eps/eps_arrow, jump-filter).
  - [x] Optional arrow-col list + monotone transform options.
- [x] Update COMPACTIFIED_CONTEXT.md with new capabilities.
- [x] Update TODO.md with new script + outputs (after implementation).

## Definition of Done

- New script runs on `hepdata_lyapunov_test_out_all/...` and produces interval-length outputs.
- Indefinite masks enforced for closure reports.
- Documentation updated with new metrics and outputs.
